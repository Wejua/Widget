# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HotWidget' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HotWidget
  pod 'SwiftUIPager', '~> 2.5.0'
  pod 'DeviceKit', '~> 5.0'

end

target 'HotWidgetIntentsExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HotWidgetIntentsExtension

end

target 'HotWidgetsExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'DeviceKit', '~> 5.0'

  # Pods for HotWidgetsExtension

end
